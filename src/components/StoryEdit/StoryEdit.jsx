// import { useStore } from '../../Utility/Hooks/useStore';

export const StoryEdit = ({ id, storyId }) => {
  // const { story } = useStore('StoryEdit');

  return `StoryEdit - ${id} - ${storyId}`;
};
